[build]
  command = "npm install && npm run build:css && hugo --gc --minify"
  publish = "public"

  [build.environment]
    HUGO_VERSION = "0.145"
    # Можете также попробовать явно указать версию Node.js, например:
    NODE_VERSION = "20.18.1"
