[build]
  # Устанавливаем только production зависимости (пропускаем devDependencies и optionalDependencies)
  install = "npm ci --production"
  # Собираем сайт с нужными флагами
  command = "hugo --gc --minify"
  # Указываем директорию публикации
  publish = "public"