[build]
  command = "npm ci && npx tailwindcss -i ./assets/css/style.css -o ./static/css/style.css --minify && hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.145"
  NODE_VERSION = "20.18.1"
